//sample code, of course this is already implemented in the speed.magnitude method
var magnitude = Math.sqrt(pointer.speed.x*pointer.speed.x + pointer.speed.y* pointer.speed.x);

var doo = document.getElementById('id');
doo.style
